# Configurações do Ambiente de Desenvolvimento
NODE_ENV=development

# Porta do servidor (opcional - padrão: 5000)
PORT=5000

# Configurações de banco de dados (opcional - usando LocalStorage por padrão)
# DATABASE_URL=postgresql://user:password@localhost:5432/catalogo
# PGHOST=localhost
# PGPORT=5432
# PGUSER=user
# PGPASSWORD=password
# PGDATABASE=catalogo

# Configurações de sessão (opcional)
# SESSION_SECRET=your-secret-key-here

# Configurações de autenticação OAuth (opcional)
# ISSUER_URL=https://replit.com/oidc
# REPL_ID=your-repl-id
# REPLIT_DOMAINS=your-domain.com